{
  "name": "worker",
  "version": "1.0.0",
  "description": "Background job worker for the NComputing India store (emails)",
  "main": "dist/src/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -b",
    "start": "node dist/src/index.js",
    "check-types": "tsc --noEmit"
  },
  "license": "ISC",
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/types": "workspace:*",
    "bullmq": "^5.34.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.17.9",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
