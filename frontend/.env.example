# Base URL of the Express API. Used by server components and the /api proxy.
API_URL="http://localhost:4000"

# MUST be byte-for-byte identical to the backend's JWT_SECRET — Next.js
# middleware verifies the same token the API signed.
JWT_SECRET="change-me-to-a-long-random-string"
